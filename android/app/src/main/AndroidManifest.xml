<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />

    <!-- Cleartext is denied globally; network_security_config.xml permits it
         only for the embedded loopback server (127.0.0.1/localhost). -->
    <application
        android:name=".FinTrackApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinTrackPK"
<<<<<<< HEAD
        android:networkSecurityConfig="@xml/network_security_config"
=======
        android:usesCleartextTraffic="false"
>>>>>>> cf5955ab49e83f742b37cfff8091bf38565c16bf
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- PIN Authentication Activity -->
        <activity
            android:name=".ui.PinAuthenticationActivity"
            android:exported="false"
            android:theme="@style/Theme.FinTrackPK.NoActionBar" />

        <!-- Settings Activity -->
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:label="@string/settings"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Debug Activity -->
        <activity
            android:name=".ui.DebugActivity"
            android:exported="false"
            android:label="@string/debug_logs"
            android:parentActivityName=".ui.SettingsActivity" />

<<<<<<< HEAD
        <!-- OAuth Callback Activity.
             NOT registered on the redirect scheme: AppAuth's
             RedirectUriReceiverActivity (declared by the appauth library on
             ${appAuthRedirectScheme}) must be the ONLY receiver of the
             browser redirect — it matches the redirect to the in-flight
             request (PKCE verifier) and launches this activity via the
             completion PendingIntent with the AppAuth response extras.
             Registering this activity on the scheme too makes the two race
             for the redirect, and a raw VIEW intent here carries no AppAuth
             extras → "Authorization failed: No response received". -->
        <activity
            android:name=".ui.OAuthCallbackActivity"
            android:exported="false"
            android:launchMode="singleTask" />
=======
        <!-- OAuth Callback Activity. The scheme comes from the per-variant
             appAuthRedirectScheme Gradle placeholder so dev and release
             builds claim distinct callback identities. -->
        <activity
            android:name=".ui.OAuthCallbackActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="${appAuthRedirectScheme}"
                    android:host="oauth2callback" />
            </intent-filter>
        </activity>
>>>>>>> cf5955ab49e83f742b37cfff8091bf38565c16bf

        <!-- The AppAuth library registers its own receiver for the same
             redirect scheme; remove it so OAuthCallbackActivity (which
             enforces state binding + PKCE) is the single callback entry
             point and no ambiguous-resolution chooser can appear. -->
        <activity
            android:name="net.openid.appauth.RedirectUriReceiverActivity"
            tools:node="remove" />

        <!-- File Provider for sharing files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
